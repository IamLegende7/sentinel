I'm bored, so I added this. You can write your own default.jsonc, so that you can set coustom paths to textures.

However, be careful; 

Everything gets overwritten; this isn't minecraft where all things you just don't set are kept form the vanilla. (at least for now)
Also you can also change some other stuff like tile hp, hitboxes, etc.. (i might change this later too)

See programer.jsonc for example.