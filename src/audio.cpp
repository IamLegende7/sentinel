//#include <SDL3/SDL.h>

//void audioCallback(void *userdata, Uint8 *stream, int len) {
//    SDL_memset(stream, 0, len); // Fill the buffer with silence
//}

//desiredSpec.callback = audioCallback; // Set the callback function
